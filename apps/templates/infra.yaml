apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra
  namespace: default
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  project: default
  source:
    path: charts/infra
    repoURL: https://github.com/ivan-clovr/argotest.git
    targetRevision: HEAD
    helm:
      parameters: []
      releaseName: infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true